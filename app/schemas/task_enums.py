from enum import Enum


class StatusTask(Enum):
    PENDING = "pending"
    COMPLETED = "completed"
    IN_PROGRESS = "in progress"

class StatusColorTask(Enum):
    PENDING = "pending"
    COMPLETED = "completed"
    IN_PROGRESS = "in progress"
